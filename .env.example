# .env da raiz do projeto — lido pelo backend Flask (chatbot.py via python-dotenv).
# Copie este arquivo para ".env" na raiz e preencha com sua chave real.
# NUNCA commite o .env de verdade (ele já está no .gitignore).

OPENAI_API_KEY=sk-coloque-sua-chave-aqui
